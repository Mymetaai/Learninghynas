import ScreenPlaceholder from '../components/ScreenPlaceholder';

// First-run welcome + setup.
const OnboardingScreen = () => (
  <ScreenPlaceholder
    title="Onboarding"
    builtIn="later"
    description="First-run welcome. Will introduce the journal, the companion, and the first quest."
  />
);
export default OnboardingScreen;
