import ScreenPlaceholder from '../components/ScreenPlaceholder';

const SpeakingScreen = () => (
  <ScreenPlaceholder
    title="Speaking Challenge"
    builtIn="Step 15"
    description="Mic recording with live waveform, Web Speech API transcription, and feedback with underlined mispronunciations + a plain-language tip."
  />
);
export default SpeakingScreen;
