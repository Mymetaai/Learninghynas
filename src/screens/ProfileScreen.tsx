import ScreenPlaceholder from '../components/ScreenPlaceholder';

const ProfileScreen = () => (
  <ScreenPlaceholder
    title="Profile & Achievements"
    builtIn="Step 11"
    description="Avatar “packing list” customization (buyable with coins) and a stamp-collection page of achievements — earned in full color, unearned as faint outlines."
  />
);
export default ProfileScreen;
